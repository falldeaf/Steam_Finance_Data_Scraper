{
  "name": "GetSteamStats",
  "version": "1.0.0",
  "description": "Scrape sites for analytics data, return as json",
  "main": "index.js",
  "scripts": {
    "start": "functions-framework --target=getSteamStats",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "falldeaf (Tom Mardis)",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0-rc.5",
    "dotenv": "^8.2.0",
    "node-fetch": "^2.6.1",
    "puppeteer-core": "^7.0.4"
  },
  "devDependencies": {
    "chrome-aws-lambda": "^7.0.0",
    "puppeteer": "^8.0.0"
  }
}
